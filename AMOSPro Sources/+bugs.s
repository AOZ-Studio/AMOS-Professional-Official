
BUGS AMOSPRO II
~~~~~~~~~~~~~~~
(*) Editor crashe avec des floats
(*) Flush crashe dans getbob
(*) AutoTest ramene un mauvais numero d'erreur
(*) Multiplication entiere
(*) Voir PRUN en cas d'erreur dans le test
(*) Sortie du moniteur
(*) =MATCH / =ARRAY retourne le type du tableau!!!
(*) FLUSH memory avec equates charges
( ) Set Accessory pas si RUN et pas PRUN
(*) Screen Logic / Screen Physic : Wonderland / Quatro
( ) Editeur bloc non tokenises
(*) Hide fenetre en haut
(*) AMOS1.3/Programs/Demo_Coll.AMOS
(*) Extras/Sprite600
(*) AMOS1.3/Programs/Shoot-em-up.AMOS
(*) Magic Forest
( ) If : Else If
( ) Del Sprite multiple
( ) Bank to menu laisse un chunk memoire

BUGS AMOSPRO COMPILER
~~~~~~~~~~~~~~~~~~~~~
( ) Faire PrgIn avec double 
(*) Verifier une garbage dans l'entree d'une procedure
(*) Verifier test =STRUC$ dans le compilateur
( ) Verifier les includes dans le compilateur
( ) Tiny Shell: compiler unprogramme vide
( ) APCmp: error test Compiler_Shell_Bug.AMOS
( ) CloseWindow: parametre=2
( ) Rajouter .AMOS si pas trouve source
( ) Compile TO Ram: ...
( ) =screen
